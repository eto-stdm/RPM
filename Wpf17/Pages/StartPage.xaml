<Page x:Class="Wpf17.Pages.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Wpf17.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StartPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"> </RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"></ColumnDefinition>
            <ColumnDefinition Width="530"></ColumnDefinition>
            <ColumnDefinition Width="130"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button x:Name="ProductsBtn" Grid.Column="0" Grid.Row="0" Content="Товары" Margin="10" Height="30" Width="100" Click="ProductsBtn_Click"/>
        <TextBlock Text="Типы услуг" Grid.Column="1" Grid.Row="0" FontSize="30" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Button x:Name="AccountBtn" Grid.Column="2" Grid.Row="0" Content="Аккаунт" Margin="10" Height="30" Width="100" Click="AccountBtn_Click"/>
        <Button x:Name="ToAuthBtn" Grid.Column="0" Grid.Row="1" Content="К авторизации" VerticalAlignment="Bottom" Margin="10" Height="30" Width="100" Click="ToAuthBtn_Click"/>
        
        <ListBox x:Name="ServiceTypesLB" MouseDoubleClick="ServiceTypesLB_MouseDoubleClick" Grid.Column="1" Grid.Row="1" ItemsSource="{Binding Path=serviceTypes}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VerticalAlignment="Center" Margin="10" Height="280">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Height="50" Width="470" Margin="10">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Name}" Margin="0, 10, 10, 10"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Page>
