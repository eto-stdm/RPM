<Page x:Class="Wpf17.Pages.RecordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Wpf17.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RecordPage">

    <Grid>
        <StackPanel>
            <TextBlock x:Name="PageHeaderTB" FontSize="30" Margin="10" HorizontalAlignment="Center"/>

            <ListBox x:Name="MastersLB" Grid.Column="1" Grid.Row="1" ItemsSource="{Binding Path=serviceTypes}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VerticalAlignment="Center" Margin="10" Height="280">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="50" Width="470" Margin="10">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Name}" Margin="0, 10, 10, 10"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>

            <Button x:Name="BackBtn" Content="Назад" Margin="10" Height="30" Width="100" Click="BackBtn_Click"/>
        </StackPanel>
    </Grid>
</Page>
